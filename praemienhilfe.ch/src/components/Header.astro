---
// src/components/Header.astro
import { deutschschweizCantons } from '../data/deutschschweiz.js';

export interface Props {
  activePage?: 'home' | 'basel-stadt' | 'basel-landschaft' | 'canton' | 'ablauf' | 'faq' | 'kontakt';
}
const { activePage = 'home' } = Astro.props;
const isCantonLanding = activePage === 'basel-stadt' || activePage === 'basel-landschaft';
const cantonFunnelHref = activePage === 'basel-stadt' ? '#funnel' : '#pruefen';
const showWerWirSind = activePage === 'basel-stadt';
// Pages that render their own #so-funktioniert-es / #faq / #pruefen sections
// (home + generic canton pages) link to those in-page; every other page
// (dedicated /so-funktioniert-es, /faq, /kontakt, basel-stadt, basel-landschaft)
// falls back to the homepage's copy of that section.
const hasOwnSections = activePage === 'home' || activePage === 'canton';
const pruefenHref = hasOwnSections ? '#pruefen' : '/#pruefen';
const sectionHref = (id: string) => (hasOwnSections ? `#${id}` : `/#${id}`);

const navLink = (key: string) =>
  activePage === key
    ? 'text-dark border-b-2 border-teal pb-1'
    : 'text-dark hover:text-teal pb-1 border-b-2 border-transparent';
---

<header class="fixed top-0 w-full z-50 bg-white border-b-2 border-teal transition-transform duration-300 translate-y-0">
  <div class="max-w-[1240px] mx-auto px-8 py-3.5 flex items-center justify-between gap-8">
    <a
      href="/"
      class="flex items-center gap-3 no-underline"
      {...isCantonLanding ? { target: '_blank', rel: 'noopener noreferrer' } : {}}
    >
      <svg width="40" height="40" viewBox="0 0 40 40" aria-hidden="true">
        <rect x="2" y="2" width="24" height="24" rx="6" fill="#0087A0"></rect>
        <rect x="14" y="14" width="24" height="24" rx="6" fill="#F0A500"></rect>
        <rect x="14" y="14" width="12" height="12" fill="#005F73"></rect>
      </svg>
      <span class="block leading-tight">
        <span class="block text-[19px] font-bold text-dark tracking-tight">prämienhilfe</span>
        <span class="block text-[15px] font-medium text-teal tracking-wide">.ch</span>
      </span>
    </a>

    {isCantonLanding ? (
      <nav class="hidden [@media(min-width:980px)]:flex items-center gap-6 text-[14.5px] font-medium">
        <a href="#so-funktioniert-es" class="text-dark hover:text-teal pb-1 border-b-2 border-transparent">So funktioniert es</a>
        <a href="#faq" class="text-dark hover:text-teal pb-1 border-b-2 border-transparent">FAQ</a>
        {showWerWirSind && <a href="#wer-wir-sind" class="text-dark hover:text-teal pb-1 border-b-2 border-transparent">Über uns</a>}
        <a href={cantonFunnelHref} class="px-4 py-2.5 bg-teal text-white rounded font-semibold hover:bg-teal-dark hover:text-white no-underline">Antrag prüfen →</a>
      </nav>
    ) : (
      <nav class="hidden [@media(min-width:980px)]:flex items-center gap-6 text-[14.5px] font-medium">
        <a href="/" class={navLink('home')}>Prämienverbilligung</a>

        <div class="relative">
          <button
            type="button"
            id="kantone-menu-trigger"
            aria-expanded="false"
            aria-controls="kantone-menu-panel"
            class="flex items-center gap-1.5 bg-transparent border-0 p-0 cursor-pointer text-[14.5px] font-medium text-dark hover:text-teal"
          >
            <span>Kantone</span>
            <span id="kantone-menu-caret" class="text-[11px] text-teal transition-transform duration-150">▾</span>
          </button>
          <div
            id="kantone-menu-panel"
            class="hidden fixed inset-x-0 top-[var(--header-h)] bg-white border-t border-[#E2E8EA] shadow-[0_16px_32px_rgba(26,26,42,0.12)]"
          >
            <div class="max-w-[1240px] mx-auto px-8 py-8">
              <h3 class="text-[12px] font-semibold uppercase tracking-wide text-[#6B7780] m-0 mb-4">Deutschschweiz</h3>
              <div class="grid grid-cols-4 gap-3">
                {
                  deutschschweizCantons.map((c) => (
                    <a
                      href={`/${c.slug}`}
                      onclick={`if (window.gtag) window.gtag('event', 'header_canton_menu_selected', { canton: '${c.slug}' })`}
                      class="flex items-center gap-3 bg-white border border-[#E2E8EA] rounded-[10px] px-4 py-3 no-underline transition-colors hover:border-teal hover:bg-teal-light"
                    >
                      <img src={c.wappen} alt="" width="26" height="26" class="flex-shrink-0" loading="lazy" />
                      <span class="text-[14px] font-medium text-dark leading-tight">{c.name}</span>
                    </a>
                  ))
                }
              </div>
            </div>
          </div>
        </div>

        <a href={sectionHref('so-funktioniert-es')} class={navLink('ablauf')}>So funktioniert es</a>
        <a href={sectionHref('faq')} class={navLink('faq')}>FAQ</a>
        <a href="/kontakt" class={navLink('kontakt')}>Kontakt</a>
        <a href={pruefenHref} class="px-4 py-2.5 bg-teal text-white rounded font-semibold hover:bg-teal-dark hover:text-white no-underline">Antrag prüfen</a>
      </nav>
    )}

    <label
      for="mobile-menu-toggle"
      class="[@media(min-width:980px)]:hidden flex items-center justify-center w-[42px] h-[42px] bg-white border border-[#D6DFE2] rounded cursor-pointer"
      aria-label="Menü öffnen"
    >
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#1A1A2A" stroke-width="2" stroke-linecap="round" aria-hidden="true">
        <path d="M4 7h16"></path><path d="M4 12h16"></path><path d="M4 17h16"></path>
      </svg>
    </label>
  </div>
</header>

<input type="checkbox" id="mobile-menu-toggle" class="hidden peer" />
<div class="fixed inset-0 z-[60] bg-white overflow-y-auto hidden peer-checked:block">
  <div class="flex items-center justify-between px-6 py-[18px] border-b border-[#E2E8EA]">
    <div class="flex items-center gap-[11px]">
      <svg width="34" height="34" viewBox="0 0 40 40" aria-hidden="true">
        <rect x="2" y="2" width="24" height="24" rx="6" fill="#0087A0"></rect>
        <rect x="14" y="14" width="24" height="24" rx="6" fill="#F0A500"></rect>
        <rect x="14" y="14" width="12" height="12" fill="#005F73"></rect>
      </svg>
      <span class="text-[17px] font-bold">prämienhilfe<span class="text-teal">.ch</span></span>
    </div>
    <label for="mobile-menu-toggle" class="w-10 h-10 flex items-center justify-center border border-[#D6DFE2] rounded cursor-pointer text-xl text-dark" aria-label="Menü schliessen">×</label>
  </div>
  {isCantonLanding ? (
    <div class="px-6 py-7 grid gap-1">
      <a href="#so-funktioniert-es" class="py-3.5 px-0.5 text-[19px] font-semibold text-dark border-b border-[#EEF2F3] hover:text-teal no-underline">So funktioniert es</a>
      <a href="#faq" class="py-3.5 px-0.5 text-[19px] font-semibold text-dark border-b border-[#EEF2F3] hover:text-teal no-underline">FAQ</a>
      {showWerWirSind && <a href="#wer-wir-sind" class="py-3.5 px-0.5 text-[19px] font-semibold text-dark border-b border-[#EEF2F3] hover:text-teal no-underline">Über uns</a>}
      <a href={cantonFunnelHref} class="mt-[18px] text-center py-4 bg-teal text-white rounded-md text-[16.5px] font-bold no-underline">Antrag prüfen →</a>
    </div>
  ) : (
    <div class="px-6 py-7 grid gap-1">
      <a href="/" class="py-3.5 px-0.5 text-[19px] font-semibold text-dark border-b border-[#EEF2F3] hover:text-teal no-underline">Prämienverbilligung</a>
      <a href="/#kantone-grid" class="py-3.5 px-0.5 text-[19px] font-semibold text-dark border-b border-[#EEF2F3] hover:text-teal no-underline">Alle Kantone</a>
      <a href="/so-funktioniert-es" class="py-3.5 px-0.5 text-[19px] font-semibold text-dark border-b border-[#EEF2F3] hover:text-teal no-underline">So funktioniert es</a>
      <a href="/faq" class="py-3.5 px-0.5 text-[19px] font-semibold text-dark border-b border-[#EEF2F3] hover:text-teal no-underline">FAQ</a>
      <a href="/kontakt" class="py-3.5 px-0.5 text-[19px] font-semibold text-dark border-b border-[#EEF2F3] hover:text-teal no-underline">Kontakt</a>
      <a href={pruefenHref} class="mt-[18px] text-center py-4 bg-teal text-white rounded-md text-[16.5px] font-bold no-underline">Antrag prüfen lassen →</a>
    </div>
  )}
</div>

<script>
  function initHeader() {
    const header = document.querySelector('header');
    if (!header) return;

    function setHeaderHeightVar() {
      document.documentElement.style.setProperty('--header-h', `${header.offsetHeight}px`);
    }
    setHeaderHeightVar();
    window.addEventListener('resize', setHeaderHeightVar);

    const mobileMenuToggle = document.getElementById('mobile-menu-toggle');
    if (mobileMenuToggle instanceof HTMLInputElement) {
      document.querySelectorAll('#mobile-menu-toggle ~ div a').forEach((link) => {
        link.addEventListener('click', () => {
          mobileMenuToggle.checked = false;
        });
      });
    }

    let lastScrollY = window.scrollY;
    window.addEventListener(
      'scroll',
      () => {
        const currentScrollY = window.scrollY;
        if (currentScrollY < 10) {
          header.classList.remove('-translate-y-full');
          header.classList.add('translate-y-0');
        } else if (currentScrollY > lastScrollY) {
          header.classList.add('-translate-y-full');
          header.classList.remove('translate-y-0');
        } else {
          header.classList.remove('-translate-y-full');
          header.classList.add('translate-y-0');
        }
        lastScrollY = currentScrollY;
      },
      { passive: true }
    );
  }

  function initKantoneMenu() {
    const trigger = document.getElementById('kantone-menu-trigger');
    const panel = document.getElementById('kantone-menu-panel');
    const caret = document.getElementById('kantone-menu-caret');
    if (!trigger || !panel || !caret) return;

    // Trigger and panel are separated by the header's own padding, so a
    // straight mouse move from one to the other briefly crosses space that
    // belongs to neither element. A short close delay bridges that gap
    // instead of hiding the panel mid-transit (which made it impossible to
    // ever reach a canton link).
    let closeTimer: ReturnType<typeof setTimeout> | null = null;

    function open() {
      if (closeTimer) {
        clearTimeout(closeTimer);
        closeTimer = null;
      }
      panel.classList.remove('hidden');
      trigger.setAttribute('aria-expanded', 'true');
      caret.classList.add('rotate-180');
    }

    function scheduleClose() {
      closeTimer = setTimeout(() => {
        panel.classList.add('hidden');
        trigger.setAttribute('aria-expanded', 'false');
        caret.classList.remove('rotate-180');
      }, 250);
    }

    trigger.addEventListener('mouseenter', open);
    trigger.addEventListener('mouseleave', scheduleClose);
    panel.addEventListener('mouseenter', open);
    panel.addEventListener('mouseleave', scheduleClose);
    trigger.addEventListener('focus', open);
    panel.addEventListener('focusout', (e) => {
      const next = (e as FocusEvent).relatedTarget;
      if (next instanceof Node && (panel.contains(next) || next === trigger)) return;
      scheduleClose();
    });
  }

  initHeader();
  initKantoneMenu();
</script>
